# 多终端处理文章并合并到主文件

## 📋 概述

当你需要在多个终端同时处理微信文章时，使用这个方案可以避免数据冲突，并将所有终端的处理结果合并到主文件。

## 🔄 工作流程

```
┌─────────────────────────────────────────────────────────────┐
│  终端 A (trae)                                               │
│  1. 处理文章 → 2026-05-25_trae.json                         │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  终端 B (tbox)                                               │
│  1. 处理文章 → 2026-05-25_tbox.json                         │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  合并脚本                                                    │
│  _merge_terminals_to_master.py                              │
│  ↓                                                           │
│  合并所有终端数据 → data/stocks/stocks_master.json          │
│  + 创建日期分片 → data/stocks/2026-05-25.json               │
└─────────────────────────────────────────────────────────────┘
```

## 🚀 使用步骤

### 1. 在不同终端处理文章

**终端 A (trae):**
```bash
# 使用 skill 处理文章（自动使用终端标识）
python .trae/skills/wechat-fetch-research-embedded/scripts/incremental_update.py \
  --mode merge \
  --json "data/stocks_master_2026-05-25.json"
```

**终端 B (tbox):**
```bash
# 使用 skill 处理文章（自动使用终端标识）
python .trae/skills/wechat-fetch-research-embedded/scripts/incremental_update.py \
  --mode merge \
  --json "data/stocks_master_2026-05-25.json"
```

### 2. 合并所有终端数据

```bash
# 合并所有终端的日期分片文件到主文件
python _merge_terminals_to_master.py
```

### 3. 查看合并结果

合并脚本会：
- ✅ 合并所有终端的第一层字段（products, core_business, industry_position 等）
- ✅ 去重文章（按 source + title）
- ✅ 更新股票的最后更新时间
- ✅ 创建日期分片文件（只包含当天更新的股票）
- ✅ 生成统计报告

## 📁 文件结构

```
e:\github\stock-research-backup\
├── .trae/skills/wechat-fetch-research-embedded/data/master/stocks/
│   ├── 2026-05-25_trae.json      # 终端 A 的数据
│   ├── 2026-05-25_tbox.json      # 终端 B 的数据
│   └── 2026-05-24.json           # 默认终端的数据
├── data/stocks/
│   ├── stocks_master.json        # 主文件（合并后）
│   ├── 2026-05-25.json           # 日期分片（合并后）
│   └── ...
└── _merge_terminals_to_master.py # 合并脚本
```

## 🔧 合并逻辑

### 第一层字段合并

```python
# 列表字段：合并去重
for field in ['products', 'core_business', 'industry_position', 'chain', 'partners', 'concepts']:
    existing_set = set(existing_stock.get(field, []))
    new_set = set(new_stock.get(field, []))
    merged[field] = list(existing_set | new_set)

# 字符串字段：直接覆盖
for field in ['name', 'code', 'board', 'industry']:
    if new_stock.get(field):
        merged[field] = new_stock[field]
```

### 文章去重

```python
# 按 source + title 去重
key = (article.get('source', ''), article.get('title', ''))
if key not in seen:
    result.append(article)
```

## 📊 统计报告

合并脚本会输出详细统计：

```
================================================================================
📊 合并结果:
   处理日期数：3
   涉及终端：trae, tbox, default
   处理股票数：23
   新增股票：1 只
   更新股票：20 只
   新增文章：21 篇
   总股票数：3199 只
   完整第一层信息：595 只 (18.6%)

✅ 完成!
================================================================================
```

## 🎯 Skill 更新

已更新 `wechat-fetch-research-embedded` skill 的第一层提取逻辑：

### 新增字段

- **industry_position**: 行业地位、竞争优势、市场排名
  - 示例：["国产交换芯片龙头", "市占率领先", "全球市占率第一"]

### 优化提取规则

```python
SYSTEM_EXTRACT = (
    "抽取规则：\n"
    "- accidents：只写事实事件，单条<=60 字。\n"
    "- insights：保留原文或忠实改写。\n"
    "- key_metrics：关键财务指标、业务数据（营收、利润、市占率等）。\n"
    "- target_valuation：目标市值、PE、估值区间。\n"
    "- products：主要产品、服务、技术（2-3 个关键词）。\n"
    "- core_business：核心业务、主营业务（1-2 句话）。\n"
    "- industry：所属行业（如\"电子 - 半导体 - 集成电路\"）。\n"
    "- industry_position：行业地位、竞争优势、市场排名（如\"龙头\"、\"领先\"、\"市占率第一\"）。\n"
    "- partners：主要客户、供应商、合作伙伴（公司名称）。\n"
    "- chain：产业链位置（如\"上游-原材料\"、\"中游-芯片设计\"）。\n"
    "- 如果文章中没有某字段信息，返回空列表或空字符串。\n"
    "- 只输出 JSON（不要解释、不要 markdown）。\n"
)
```

## 💡 最佳实践

1. **定期合并**：每天处理完文章后，运行合并脚本
2. **终端标识**：使用不同的终端标识（如 trae, tbox）避免冲突
3. **备份主文件**：合并前备份 `stocks_master.json`
4. **验证数据**：合并后检查统计报告，确认数据正确

## 🆘 常见问题

### Q1: 如何指定终端标识？

A: 在 `incremental_update.py` 中添加 `--terminal` 参数：

```bash
python .trae/skills/wechat-fetch-research-embedded/scripts/incremental_update.py \
  --mode merge \
  --json "data/stocks_master_2026-05-25.json" \
  --terminal "trae"
```

### Q2: 如何只合并特定日期的数据？

A: 修改 `_merge_terminals_to_master.py`，添加日期过滤：

```python
sorted_dates = [d for d in sorted_dates if d >= "2026-05-20"]
```

### Q3: 如何处理数据冲突？

A: 合并脚本会自动去重文章，第一层字段使用集合合并去重。如果需要手动处理冲突，可以修改 `merge_first_level_fields` 函数。

### Q4: 合并后如何同步到 Firebase？

A: 运行 Firebase 同步脚本：

```bash
python sync_stocks_to_firebase.py --json data/stocks/stocks_master.json
```

## 📝 相关文件

- `_merge_terminals_to_master.py` - 合并脚本
- `.trae/skills/wechat-fetch-research-embedded/scripts/incremental_update.py` - 增量更新脚本
- `.trae/skills/wechat-fetch-research-embedded/scripts/extract_stocks_from_raw_material.py` - 提取脚本（已更新）
- `数据结构说明_个股 + 分组 + 热点.md` - 数据结构文档