# 项目文件夹结构说明

**版本**: v2.9 | **更新**: 2026-06-21

## 📁 目录结构

```
stock-research-backup/
│
├── 📂 .trae/                          # Trae IDE 配置和技能
│   ├── rules/                         # 规则配置
│   └── skills/                        # 技能脚本
│       ├── add-hot-topic/             # 热点管理技能
│       └── wechat-fetch-research-embedded/  # 微信文章处理技能 (v2.9)
│           ├── assets/                # 静态资源 (全部个股.xls)
│           ├── raw_material/          # 原始素材
│           ├── scripts/               # 处理脚本
│           │   ├── pipeline.py              # 主流程编排
│           │   ├── extract_stocks.py        # LLM 抽取个股 (本地扫描+上下文提取)
│           │   ├── merge_new_stocks.py      # 合并到主文件 + 自动生成 .gz
│           │   ├── merge_utils.py           # 共享合并/日期规范化/ gz 生成
│           │   ├── sync_to_supabase.py      # Supabase 同步 (全量/增量)
│           │   ├── normalize_dates.py       # 日期格式规范化 (YYYY-MM-DD)
│           │   └── sync_to_github.py        # GitHub 同步
│           └── SKILL.md
│
├── 📂 data/                           # 数据文件（核心）
│   ├── 📂 stocks/                     # 个股数据
│   │   ├── stocks_master.json         # 完整股票数据（3506 只）⚠️ 前端读取
│   │   ├── stocks_master.json.gz      # 压缩版（Vercel 优先读取，~914KB）
│   │   ├── stocks_index.json          # 股票索引
│   │   ├── YYYY-MM-DD.json            # 按日期分片（增量更新）
│   │   └── value_YYYY-MM-DD.json      # 深度研报批量处理输出
│   ├── 📂 groups/                     # 分组数据
│   │   └── groups.json
│   └── 📂 hot_topics/                 # 热点数据
│       └── hot_topics.json
│
├── 📂 templates/                      # Web 界面模板
│   ├── dashboard.html                 # 仪表板
│   ├── stocks.html / stock_detail.html
│   └── ...
│
├── 📄 main.py                         # Flask 主程序 (Vercel: Supabase HTTP API → GitHub raw)
├── 📄 _lobster_quick.py               # 龙虾终端快速处理工具 (7步全自动)
├── 📄 _regz.py                        # json.gz 修复/重新生成工具
├── 📄 requirements.txt                # Python 依赖
├── 📄 vercel.json                     # Vercel 部署配置
├── 📄 Dockerfile                      # Docker 部署配置
│
└── 📄 README.md                       # 项目说明
```

## 📊 核心文件说明

### 数据文件（data/）

| 文件 | 说明 | 大小 | 更新频率 |
|------|------|------|----------|
| `stocks_master.json` | 完整股票数据（3506 只） | ~5.1MB | 每日 |
| `stocks_master.json.gz` | 压缩版（Vercel 优先读取） | ~914KB | 每日自动 |
| `stocks_index.json` | 股票索引（快速查找） | ~1MB | 每日 |
| `groups.json` | 股票分组 | ~2KB | 按需 |
| `hot_topics.json` | 热点主题 | ~50KB | 按需 |

### 核心脚本

| 脚本 | 功能 |
|------|------|
| `_lobster_quick.py` | 龙虾终端一站处理（抓取→抽取→合并→Git→Supabase） |
| `_regz.py` | 修复/重新生成 stocks_master.json.gz |
| `.trae/.../merge_new_stocks.py` | 合并到主文件 + 自动规范化日期 + 自动生成 .gz |
| `.trae/.../sync_to_supabase.py` | Supabase 同步（全量/增量） |
| `.trae/.../normalize_dates.py` | 日期格式检查和修复 |

### Web 模板（templates/）

| 模板 | 用途 |
|------|------|
| `dashboard.html` | 主页仪表板 |
| `stocks.html` | 股票列表页 |
| `stock_detail.html` | 个股详情页 |
| `group_detail.html` | 分组详情页 |
| `hot_topic_detail.html` | 热点详情页 |

## 🗑️ 临时文件（不提交到 Git）

以下文件夹包含浏览器缓存等临时文件，已通过 `.gitignore` 排除：

- `.browser_profile/` - 浏览器配置文件
- `raw_material/.browser_profile/` - 临时浏览器缓存
- `*.html` (临时下载) - 临时 HTML 文件
- `ourcesappoutvsworkbenchcontribterminalcommonscriptsshellIntegration.ps1` - 终端临时文件

## 📝 文件整理规则

### ✅ 保留的文件

1. **核心数据**: `data/master/` 下的所有 JSON 文件
2. **脚本**: `.trae/skills/` 下的技能脚本
3. **模板**: `templates/` 下的 HTML 文件
4. **配置**: `.trae/rules/`, `vercel.json`, `.gitignore`
5. **文档**: `README.md`, `数据结构规范_v2.md`

### ❌ 删除的文件

1. **浏览器缓存**: `.browser_profile/` 所有内容
2. **临时文件**: `*.html` (临时下载), `tmp_*.txt`
3. **历史报告**: `archived/docs/` 下的部署报告
4. **旧脚本**: 已废弃的处理脚本
5. **重复数据**: 旧的 `stocks_master_*.json` 备份

## 🔄 数据流转

```
微信公众号文章 / 手动粘贴研报
    ↓ (_lobster_quick.py / pipeline.py)
raw_material/raw_material_YYYY-MM-DD.md
    ↓ (LLM 抽取 + 本地股票扫描 v2.7)
data/stocks_master_YYYY-MM-DD.json
    ↓ (merge_new_stocks.py)
data/stocks/stocks_master.json  ← 主数据文件
    ├──→ 自动生成 .json.gz (Vercel 优先读取)
    ├──→ git push GitHub
    ├──→ sync_to_supabase.py → Supabase PostgreSQL
    └──→ Vercel 部署 (Supabase HTTP API → GitHub raw 回退)

Vercel 数据加载优先级:
  ① Supabase REST API (纯 stdlib urllib, 零依赖)
  ② GitHub raw (public repo)
  ③ 本地 stocks_master.json.gz (打包进部署)
```

## 📋 清理建议

### 每日清理
- 删除临时 HTML 文件
- 清理浏览器缓存

### 每周清理
- 归档旧的 raw_material 文件
- 删除测试脚本

### 每月清理
- 压缩历史数据到 `archived/`
- 更新文档

---

### ⚠️ 重要注意事项

1. **json.gz 必须正确**: 大小~900KB，如异常（60KB）运行 `python _regz.py` 修复
2. **Python 版本**: 项目用 Python 3.13（3.14 环境有问题）
3. **Vercel 部署**: 推送代码后需手动 Redeploy
4. **日期格式**: 统一 `YYYY-MM-DD`，禁止 YYYYMMDD

---
**最后更新**: 2026-06-21
