# 股票研究系统数据结构说明

**版本**: v4.1  
**更新日期**: 2026-06-26  
**适用**: 个股 + 分组 + 热点 + 突破信号 四位一体研究系统（v3.0 数据架构）

> v4.1：移除 Firebase 同步链路，改为 GitHub + Supabase + ModelScope 三层部署
> v4.0：新增 lyt 突破信号数据（`breakt/`），三 tab 首页，stocks_meta 轻量加载

---

## 📋 目录

1. [整体架构](#整体架构)
2. [个股数据](#个股数据)
3. [分组数据](#分组数据)
4. [热点数据](#热点数据)
5. [数据同步](#数据同步)
6. [API 接口](#api 接口)
7. [使用示例](#使用示例)

---

## 🏗️ 整体架构

本系统采用**三层数据结构**：

```
┌─────────────────────────────────────┐
│     热点主题 (Hot Topics)           │  ← 宏观层面：市场热点、产业链
│  - 算电协同、超节点、固态电池...    │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│     股票分组 (Groups)               │  ← 中观层面：行业、概念、主题
│  - 锂资源、铝业、稀土...            │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│     个股研究 (Stocks)               │  ← 微观层面：具体公司
│  - 000001 平安银行、600519 贵州茅台... │
└─────────────────────────────────────┘
```

### 数据文件位置

```
app_stockapril/
├── data/
│   ├── stocks/
│   │   ├── stocks_master.json          # 个股主数据（3506 只股票）⚠️ 前端读取此文件
│   │   ├── stocks_master.json.gz       # 压缩版（Vercel 优先读取，~914KB）
│   │   ├── stocks_master.min.json      # 精简版（仅基本信息）
│   │   ├── stocks_index.json           # 股票索引（快速查找）
│   │   ├── YYYY-MM-DD.json             # 按日期分片（增量更新）
│   │   ├── value_YYYY-MM-DD.json       # 深度研报批量处理输出
│   │   └── ...
│   ├── groups/
│   │   ├── groups.json                 # 股票分组数据
│   │   └── stock_groups.json
│   ├── hot_topics/
│   │   └── hot_topics.json             # 热点主题数据
│   └── master/                         # ⚠️ 旧版路径，已废弃
├── raw_material/                       # 原始素材
│   ├── raw_material_YYYY-MM-DD.md      # 微信文章原文
│   ├── value/                          # 深度研报 HTML（离线下载）
│   ├── topdown/                        # 自上而下分析 HTML（离线下载）
│   ├── value_formatted/                # 格式化后的深度研报 MD
│   └── topdown_formatted/              # 格式化后的分析文章 MD
└── .trae/skills/                       # 自动化处理脚本

---

## 📈 个股数据

### 数据结构（v2.4）

采用**股票 - 文章两层结构**：

#### 示例：科技类个股（盛科通信）

```json
{
  "stocks": {
    "688250": {
      // 第一层：股票基本信息
      "name": "盛科通信",
      "code": "688250",
      "board": "SH",
      "industry": "电子 - 半导体 - 集成电路",
      "concepts": ["交换芯片", "Scale-out", "Scale-up", "国产芯片"],
      "products": ["交换芯片"],
      "core_business": ["交换芯片研发与销售"],
      "industry_position": ["国产交换芯片龙头"],
      "chain": ["中游 - 芯片设计"],
      "partners": ["阿里巴巴", "字节跳动", "腾讯"],
      "mention_count": 2,
      "last_updated": "2026-05-09",
      
      // 第二层：文章详情
      "articles": [
        {
          "title": "今天的一些信息整理 5.7",
          "date": "2026-05-09",
          "source": "https://mp.weixin.qq.com/s/jU3eFc1irDolyJyLd6egyg",
          "industry_background": ["交换芯片从配角变主角，Scale-out需求爆发驱动交换芯片出货量倍增"],
          "accidents": ["二季度订单加速放量", "阿里3万+ scale out订单意向"],
          "insights": ["MRC 协议影响有限，珍惜倒车接人机会"],
          "key_metrics": ["2026 年国产 GPU 出货量预计 400 万颗"],
          "target_valuation": ["约 2700 亿市值"]
        }
      ]
    }
  }
}
```

#### 示例：资源/矿业类个股（梅雁吉祥）

```json
{
  "stocks": {
    "600868": {
      "name": "梅雁吉祥",
      "code": "600868",
      "board": "SH",
      "industry": "有色金属 - 有色金属 - 其他稀有金属",
      "concepts": ["白银", "锑", "有色金属", "矿业", "高品位矿"],
      "products": ["银矿", "锑矿"],
      "core_business": ["银锑矿开采与销售（嵩溪银锑矿100%权益）"],
      "industry_position": [
        "嵩溪银锑矿为高品位富矿，白银品位213.6g/t远高于工业标准80-120g/t",
        "锑品位5.61%远高于工业标准1.5%"
      ],
      "chain": ["上游 - 矿产资源 - 有色金属矿采选"],
      "partners": ["华创机械（研报来源）"],
      "mention_count": 1,
      "last_updated": "2026-01-22",

      "articles": [
        {
          "title": "梅雁吉祥，160%空间？",
          "date": "2026-01-22",
          "source": "https://mp.weixin.qq.com/s/H26-LtPHCOMXn66ufrgbxA",
          "accidents": [
            "1月22日公司公告嵩溪银锑矿核心勘探成果，确认高品位银锑富矿（100%权益）",
            "预计2年内完成采矿证办理及基建工作，27年底前可投产",
            "未来可能会进一步做深部找矿增储，潜力很大"
          ],
          "insights": [
            "华创机械认为梅雁吉祥能看到160%空间，市值看200亿以上",
            "矿山满产后合计净利润15.44e，给15xPE，市值可看到230亿，现价160%以上空间",
            "白银品位213.6g/t和锑品位5.61%均显著高于行业平均水平，开采成本优势明显"
          ],
          "key_metrics": [
            "白银资源储量662.6吨，平均品位213.6g/t",
            "锑金属资源储量20990.5吨，平均品位5.61%",
            "满产后银年产64吨，锑年产量1960吨",
            "估算银完全成本2000元/kg（行业平均3000-3500元/kg）",
            "当前白银价格23000元/kg，贡献年净利润13.44亿",
            "锑吨净利10万，贡献年净利润2亿",
            "矿山满产后合计净利润15.44亿"
          ],
          "target_valuation": [
            "华创机械目标市值230亿，相对现价有160%以上空间"
          ]
        }
      ]
    }
  }
}
```

> 💡 **提示**：上述两个示例展示了同一数据结构在不同类型个股中的应用。盛科通信（688250）代表科技/半导体类，梅雁吉祥（600868）代表资源/矿业类，字段结构完全一致，表明数据模型具有良好通用性。

### 字段说明

#### 股票层级字段

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `name` | string | 股票名称 | "盛科通信" / "梅雁吉祥" |
| `code` | string | 股票代码 | "688250" / "600868" |
| `board` | string | 交易所板块 | "SH"（上海）、"SZ"（深圳） |
| `industry` | string | 所属行业（三级分类） | "电子 - 半导体 - 集成电路" / "有色金属 - 有色金属 - 其他稀有金属" |
| `concepts` | array | 所属概念 | ["交换芯片", "国产芯片"] |
| `products` | array | 主要产品 | ["交换芯片"] / ["银矿", "锑矿"] |
| `core_business` | array | 核心业务 | ["交换芯片研发与销售"] |
| `industry_position` | array | 行业地位 | ["国产交换芯片龙头"] |
| `chain` | array | 产业链位置 | ["中游 - 芯片设计"] / ["上游 - 矿产资源 - 有色金属矿采选"] |
| `partners` | array | 合作伙伴 | ["阿里巴巴", "字节跳动"] |
| `mention_count` | integer | 文章数量 | 2 |
| `valuation` | object | 估值信息 | 见下方 |
| `last_updated` | date | 最后更新日期 | "2026-05-09" |

#### 文章层级字段（v2.4 5 维度标准）

| 字段 | 类型 | 说明 | 示例 | 红线 |
|------|------|------|------|------|
| `title` | string | 文章标题 | "今天的一些信息整理5.7" | - |
| `date` | date | 文章日期 | "2026-05-09" | - |
| `source` | url | 来源链接 | 微信公众号链接 | - |
| `industry_background` | array | 行业/赛道宏观背景 | ["AI服务器出货量爆发，驱动PCB需求"] | **严禁出现公司名称** |
| `accidents` | array | 客观事件/催化剂（≤60字） | ["订单加速放量"] | 严禁主观推测词 |
| `insights` | array | 个股投研观点 | ["国产交换芯片龙头，份额30%"] | 必须指向具体个股 |
| `key_metrics` | array | 关键量化数据 | ["出货量400万颗"] | **必须包含阿拉伯数字** |
| `target_valuation` | array | 目标估值锚点 | ["约2700亿市值"] | **必须包含具体数值+单位** |

### 排序规范 ⭐️

**所有数据必须按时间降序排序**（最新的在前）：

```python
# 股票列表按 last_updated 降序
stocks.sort(key=lambda x: x.get('last_updated', ''), reverse=True)

# 文章列表按 date 降序
for stock in stocks:
    if 'articles' in stock:
        stock['articles'].sort(key=lambda x: x.get('date', ''), reverse=True)
```

### 文件说明

1. **stocks_master.json**: 完整数据（包含所有字段）
2. **stocks_master.min.json**: 精简版（仅基本信息，用于快速加载）
3. **stocks_index.json**: 索引文件（快速查找股票代码）

---

## 📦 分组数据

### 数据结构

```json
{
  "groups": [
    {
      "id": "group_20260511203601",
      "name": "锂资源",
      "description": "锂矿资源相关股票",
      "color": "#10b981",
      "icon": "⚡",
      "stocks": ["盐湖股份", "藏格矿业", "永兴材料", "金圆股份"],
      "created_at": "2026-05-11",
      "updated_at": "2026-05-11"
    },
    {
      "id": "group_20260511203602",
      "name": "铝业",
      "description": "铝业相关股票",
      "color": "#f59e0b",
      "icon": "🏭",
      "stocks": ["中国铝业", "云铝股份", "神火股份", "南山铝业", "天山铝业"],
      "created_at": "2026-05-11",
      "updated_at": "2026-05-11"
    }
  ]
}
```

### 字段说明

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `id` | string | 分组唯一 ID | "group_20260511203601" |
| `name` | string | 分组名称 | "锂资源" |
| `description` | string | 分组描述 | "锂矿资源相关股票" |
| `color` | string | 主题色（Hex） | "#10b981" |
| `icon` | string | 图标（emoji） | "⚡" |
| `stocks` | array | 股票名称列表 | ["盐湖股份", "藏格矿业"] |
| `created_at` | date | 创建日期 | "2026-05-11" |
| `updated_at` | date | 最后更新 | "2026-05-11" |

### 分组类型

分组可以按以下维度创建：

1. **行业分类**: 锂资源、铝业、稀土、半导体...
2. **概念主题**: 固态电池、AI 算力、低空经济...
3. **产业链**: 上游资源、中游制造、下游应用...
4. **自定义**: 根据研究需要自由组合

### 管理操作

通过 `add-hot-topic` 技能管理分组：

```bash
# 添加分组
python .trae/skills/add-hot-topic/scripts/add_hot_topic.py --type group --name "锂资源" --stocks "盐湖股份，藏格矿业"

# 列出分组
python .trae/skills/add-hot-topic/scripts/list_hot_topics.py --type groups

# 删除分组
python .trae/skills/add-hot-topic/scripts/delete_hot_topic.py --type group --id "group_xxx"
```

---

## 🔥 热点数据

### 数据结构

```json
{
  "topics": [
    {
      "id": "topic_20260506000000",
      "name": "算电协同",
      "drivers": "国内首个大规模算电协同项目（宁夏中卫云基地 200 万千瓦新能源项目）落地...",
      "stocks": [
        "协鑫能科",
        "晶科科技",
        "华电新能",
        "三峡能源",
        "金开新能",
        "福能股份",
        "粤电力 A",
        "美利云",
        "润泽科技",
        "数据港",
        "国电南瑞",
        "南网科技",
        "科华数据",
        "中恒电气",
        "金风科技",
        "中国能建",
        "中国电建",
        "大唐发电"
      ],
      "display": true,
      "created_at": "2026-05-06",
      "updated_at": "2026-05-06"
    },
    {
      "id": "topic_20260506000001",
      "name": "超节点（昇腾）",
      "drivers": "整机与方案厂商中科曙光（603019）：超节点绝对龙头...",
      "stocks": [
        "中科曙光",
        "浪潮信息",
        "紫光股份",
        "华勤技术",
        "拓维信息",
        "海光信息",
        "寒武纪",
        "中芯国际",
        "盛科通信",
        "华丰科技",
        "锐捷网络",
        "中际旭创",
        "英维克",
        "高澜股份",
        "欧陆通"
      ],
      "display": true,
      "created_at": "2026-05-06",
      "updated_at": "2026-05-06"
    }
  ]
}
```

### 字段说明

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `id` | string | 热点唯一 ID | "topic_20260506000000" |
| `name` | string | 热点名称 | "算电协同" |
| `drivers` | string | 驱动逻辑/催化剂 | 详细说明热点背后的逻辑 |
| `stocks` | array | 相关股票列表 | ["协鑫能科", "晶科科技"] |
| `display` | boolean | 是否显示 | true |
| `created_at` | date | 创建日期 | "2026-05-06" |
| `updated_at` | date | 最后更新 | "2026-05-06" |

### 热点类型

1. **产业链热点**: 算电协同、超节点、固态电池...
2. **政策热点**: 低空经济、数据要素、人工智能 +...
3. **事件热点**: 产品发布会、行业大会、财报季...
4. **技术热点**: 铜缆连接、液冷、HBM...

### 管理操作

通过 API 或 Web 界面管理热点数据。

```bash
# 通过 API 创建/管理热点
# 使用 Web UI: http://localhost:7860 或 https://treeie-aastock.ms.show
# 或直接编辑 data/hot_topics/hot_topics.json
```

---

## 🔄 数据同步

### 同步架构

```
本地 JSON 文件
    ↓
git push → GitHub (版本控制)
    ↓
sync_to_supabase.py → Supabase PostgreSQL (云端数据库)
    ↓
ModelScope 部署 (从 GitHub raw 或 Supabase 读取)
    ↓
Web 界面展示 (https://treeie-aastock.ms.show)
```

### 同步脚本

1. **Supabase 同步**: `sync_to_supabase.py`
2. **GitHub 同步**: `git push origin main`
3. **衍生文件**: `python build_derivatives.py` 生成 `stocks_meta.json` + `stocks_articles.json` + `stocks_master.json.gz`
4. **json.gz 修复**: `python _regz.py`

### 同步命令

```bash
# 增量同步当日变动到 Supabase
python sync_to_supabase.py --date 2026-06-26

# 全量同步到 Supabase
python sync_to_supabase.py --full

# 重建衍生文件
python build_derivatives.py

# 修复 json.gz 文件
python _regz.py
```

### 数据部署架构

```
GitHub (app_stockapril)
├── data/stocks/stocks_master.json.gz  (主数据压缩包)
├── data/stocks/stocks_meta.json       (轻量元数据)
├── data/stocks/stocks_articles.json   (文章分离)
├── data/groups/groups.json            (分组)
├── data/hot_topics/hot_topics.json    (热点)
└── breakt/                            (lyt 突破信号)

ModelScope 创空间
└── (通过 GitHub 仓库自动构建部署)
```

---

## 🌐 API 接口

### 个股 API

```
GET /api/stocks?code=688250
  获取指定股票详情

GET /api/stocks/search?q=盛科
  搜索股票

GET /api/stocks/all
  获取所有股票列表（精简版）
```

### 分组 API

```
GET /api/groups
  获取所有分组

GET /api/groups/{id}
  获取指定分组详情

POST /api/groups
  创建新分组

DELETE /api/groups/{id}
  删除分组
```

### 热点 API

```
GET /api/hot-topics
  获取所有热点

GET /api/hot-topics/{id}
  获取指定热点详情

POST /api/hot-topics
  创建新热点

DELETE /api/hot-topics/{id}
  删除热点
```

---

## 💡 使用示例

### 1. 添加新个股（完整流程）

#### 方式A：微信文章实时抓取流程

```bash
# Step 1: 从微信文章提取个股信息
python .trae/skills/wechat-fetch-research-embedded/scripts/fetch_wechat_via_browser_dom.py \
  --url "https://mp.weixin.qq.com/s/xxx" \
  --out_text "tmp_article.txt" \
  --user_data_dir ".browser_profile"

# Step 2: 转换为 raw_material 格式
python .trae/skills/wechat-fetch-research-embedded/scripts/fetch_wechat_to_raw_material.py \
  --url "https://mp.weixin.qq.com/s/xxx" \
  --out "raw_material/raw_material_YYYY-MM-DD.md" \
  --manual_text_file "tmp_article.txt"

# Step 3: 抽取个股结构化信息（需 LLM API Key）
python .trae/skills/wechat-fetch-research-embedded/scripts/extract_stocks_from_raw_material.py \
  --raw "raw_material/raw_material_YYYY-MM-DD.md" \
  --stock_xls ".trae/skills/wechat-fetch-research-embedded/assets/全部个股.xls" \
  --out_json "data/stocks_master_YYYY-MM-DD.json" \
  --mode merge

# Step 4: 合并到 stocks_master.json
python scripts/merge_new_stocks.py

# Step 5: 重启服务器
Stop-Process -Name "python" -Force
Start-Process -NoNewWindow python -ArgumentList "main.py"
```

#### 方式B：离线深度研报批量处理（推荐，无需 API）

适用于从某处下载了大批量 HTML 研报后的批量处理：

```bash
# Step 1: 将 HTML 研报放入 raw_material/value/ 或 raw_material/topdown/

# Step 2: 解析 HTML，提取纯文本，生成 raw_material 和结构化 JSON
python process_html_value.py    # 处理 value/ 目录
python process_topdown.py       # 处理 topdown/ 目录

# Step 3: 验证输出 → data/stocks/value_YYYY-MM-DD.json
# Step 4: AI 审核 JSON 内容（修正名称、行业、概念）

# Step 5: 补齐第一层字段（products/core_business/industry_position/chain/partners）
python extract_first_layer.py

# Step 6: 合并到 stocks_master.json
# （由 process_*.py 自动完成，或手动运行）

# Step 7: 推送到 GitHub
git add data/stocks/stocks_master.json && git commit -m "feat: 批量处理研报" && git push origin main
```

#### 方式C：AI 辅助手动提取（当前主要方式）

1. AI 读取文章内容
2. AI 提取结构化数据：accidents/insights/key_metrics/target_valuation
3. AI 从 Excel 映射补齐 industry/concepts
4. 直接构建 JSON 保存到 `data/stocks/YYYY-MM-DD.json`
5. 合并到 stocks_master.json 并推送

### 2. 创建新分组

```python
# 添加锂资源分组
python .trae/skills/add-hot-topic/scripts/add_hot_topic.py \
  --type group \
  --name "锂资源" \
  --description "锂矿资源相关股票" \
  --stocks "盐湖股份，藏格矿业，永兴材料" \
  --color "#10b981" \
  --icon "⚡"
```

### 3. 添加新热点

```python
# 添加算电协同热点
python .trae/skills/add-hot-topic/scripts/add_hot_topic.py \
  --type topic \
  --name "算电协同" \
  --drivers "国内首个大规模算电协同项目落地..." \
  --stocks "协鑫能科，晶科科技，华电新能..." \
  --display true
```

### 4. 查询股票所属分组

```python
import json

# 加载分组数据
with open('data/groups.json', 'r', encoding='utf-8') as f:
    groups = json.load(f)['groups']

# 查找"盐湖股份"所属分组
stock_name = "盐湖股份"
for group in groups:
    if stock_name in group['stocks']:
        print(f"{stock_name} 属于分组：{group['name']} ({group['description']})")
```

### 5. 查询热点包含的股票

```python
import json

# 加载热点数据
with open('data/hot_topics.json', 'r', encoding='utf-8') as f:
    topics = json.load(f)['topics']

# 查询"超节点"热点的股票
for topic in topics:
    if "超节点" in topic['name']:
        print(f"热点：{topic['name']}")
        print(f"驱动逻辑：{topic['drivers'][:100]}...")
        print(f"相关股票：{', '.join(topic['stocks'])}")
```

### 6. 同步数据到云端

```bash
# 1. 推 GitHub
git add data/stocks/ data/groups/ data/hot_topics/ breakt/
git commit -m "feat: update data YYYY-MM-DD"
git push origin main

# 2. 同步到 Supabase（可选）
python sync_to_supabase.py --date 2026-06-26

# 3. ModelScope 自动部署（或手动 Redeploy）
# https://modelscope.cn/studios/TREEIE/aastock
```

---

## 📊 数据统计

截至 2026-06-26：

| 数据类型 | 数量 | 文件大小 | 最后更新 |
|----------|------|----------|----------|
| 个股 | 3553 只 | ~8.5MB (json) + 1.1MB (gz) | 2026-06-26 |
| stocks_meta.json | 3553 只 | ~2.8MB | 2026-06-26 |
| stocks_articles.json | ~2200 篇 | ~2.9MB | 2026-06-26 |
| 分组 | 动态 | ~10KB | 2026-06-26 |
| 热点 | 动态 | ~5KB | 2026-06-26 |
| lyt 突破信号 | ~5207 条日记录 | ~800KB | 2026-06-26 |

### 第一层字段覆盖率

| 字段 | 覆盖 | 补齐方式 |
|------|:----:|----------|
| `industry` | 99.7% | 同花顺行业.xls 映射 + 手动补齐 |
| `concepts` | 99.9% | 所属概念.xls 映射 + 手动补齐 |
| `products` | 100% | AI 模式匹配 + master 已有数据 |
| `core_business` | 94% | AI 模式匹配 + master 已有数据 |
| `industry_position` | 92% | AI 模式匹配 + master 已有数据 |
| `chain` | 88% | AI 模式匹配 + master 已有数据 |
| `partners` | 55% | 文章本身可能未提及合作方 |
| `lyt_score` | ~30% | breakt/sc.json 五维评分 |

---

## 🔧 维护工具

### 第一层字段自动补齐

```bash
# 从 raw_material 提取 products/core_business/industry_position/chain/partners
python extract_first_layer.py

# 从同花顺 Excel 补齐行业和概念
python fill_stock_info.py

# 从 raw_material 补齐个股基础信息
python fill_from_raw.py
```

### 批量处理离线研报

```bash
# 处理 value/ 目录的深度研报 HTML
python process_html_value.py

# 处理 topdown/ 目录的分析文章 HTML
python process_topdown.py
```

### 清理重复代码

```bash
# 检查重复
python check_and_merge_duplicates.py

# 合并重复股票
python merge_688702_to_688250.py
```

### 数据验证

```bash
# 验证数据结构
python verify_data_structure.py

# 检查数据一致性
python build_derivatives.py  # 重建衍生文件并验证
```

### Git 同步

```bash
# 提交更改
git add data/
git commit -m "更新个股数据"
git push origin main

# 拉取更新
git pull origin main
```

---

## 📝 最佳实践

1. **每日更新**: 处理最新微信文章，更新个股数据
2. **每周清理**: 检查并合并重复股票代码
3. **每月归档**: 将旧数据归档到 `archived/` 目录
4. **重建衍生文件**: 每次更新后运行 `python build_derivatives.py`
5. **推 GitHub**: 提交并推送所有数据变更
6. **ModelScope Redeploy**: 推送后 ModelScope 自动或手动重建部署
7. **验证 json.gz**: 确保 `stocks_master.json.gz` 压缩正确（~1MB），不是 60KB

---

## 🆘 常见问题

### Q1: 如何处理重复股票代码？
A: 使用 `check_and_merge_duplicates.py` 检查，然后运行对应的合并脚本。

### Q2: 如何添加新的热点主题？
A: 使用 Web UI 或手动编辑 `data/hot_topics/hot_topics.json`。

### Q3: 数据太重，Flask 启动慢？
A: 自动优先加载 `stocks_meta.json`（轻量，不含 articles），详情页按需读取 `stocks_articles.json`。

### Q4: 数据文件太大怎么办？
A: 使用精简版 `stocks_master.min.json` 或按日期分片存储。

---

### Q5: ModelScope 数据不更新？
A: 检查 `stocks_master.json.gz` 是否损坏（大小应为~1MB）。运行 `python _regz.py` 修复后 git push，ModelScope 自动或手动 Redeploy。

### Q6: 行业/概念显示为"—"？
A: 运行 `python build_derivatives.py` 重建 `stocks_meta.json`（确保包含 industry/concepts 字段）。

### Q7: lyt 突破信号数据在哪？
A: `breakt/` 目录下的 `lyt.json`（信号）和 `sc.json`（五维评分），通过 `/api/lyt-*` 系列 API 访问。

---

**文档维护**: 系统自动更新  
**最后更新**: 2026-06-21
